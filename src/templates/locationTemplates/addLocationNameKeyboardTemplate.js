const {
  BUTTON_ADD_CURRENT_LOCATION,
} = require('../../constants/button.text').ADD_LOCATIONS_MENU;

const {
  BUTTON_HOME,
  BUTTON_BACK,
} = require('../../constants/button.text').COMMON;

const addLocationNameMenuKeyboardTemplate = [
  [BUTTON_HOME, BUTTON_BACK],
];

module.exports = addLocationNameMenuKeyboardTemplate;
