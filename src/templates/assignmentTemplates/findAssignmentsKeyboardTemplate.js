const { COMMON_BUTTONS } = require('../../constants/button.text');

const findAssignmentKeyboardTemplate = [
  [COMMON_BUTTONS.BACK],
  [COMMON_BUTTONS.HOME],
];

module.exports = findAssignmentKeyboardTemplate;
