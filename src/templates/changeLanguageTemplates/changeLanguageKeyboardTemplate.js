const {
  CHANGE_LANGUAGE_BUTTONS,
  COMMON_BUTTONS,
} = require('../../constants/button.text');

const changeLanguageKeyboardTemplate = [
  [
    CHANGE_LANGUAGE_BUTTONS.UA,
    CHANGE_LANGUAGE_BUTTONS.RU,
    CHANGE_LANGUAGE_BUTTONS.EN,
  ],
  [
    COMMON_BUTTONS.HOME,
  ],
];

module.exports = changeLanguageKeyboardTemplate;
